! version = 2.0


> object test python
    import pymysql
    conn = pymysql.connect(host = 'localhost',user = 'root',password ='root',charset = 'utf8',db = 'recipe_db')
    print(conn)
    sql = "select category_name from big_category where id<100"
    cursor = conn.cursor()
    cursor.execute(sql)
    result = cursor.fetchall()
    // result = result.join("\n")
    print(result)
    print("HELLO")
< object